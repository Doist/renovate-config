{
    "packageRules": [
        {
            "matchUpdateTypes": ["patch", "pin", "pin", "digest", "lockFileMaintenance"],
            "automerge": true
        },
        {
            "matchDepTypes": ["devDependencies"],
            "automerge": true
        }
    ]
}
